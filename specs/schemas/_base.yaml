name: _base
description: Base fields shared by timestamped spec files (metadata)

fields:
  - name: created
    type: datetime
    required: true
    description: ISO 8601 timestamp when the spec was created

  - name: modified
    type: datetime
    required: true
    description: ISO 8601 timestamp of last modification
